import React from 'react';

import { Components } from 'fun-plus';
const { BasePage } = Components;

export default class NewCustomer extends BasePage {
  render() {
    return (
      <div>NewCustomer</div>
    )
  }
}